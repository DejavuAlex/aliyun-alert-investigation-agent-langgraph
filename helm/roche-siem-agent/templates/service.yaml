
apiVersion: v1
kind: Service
metadata:
  name: {{ include "security-mcp-server.fullname" . }}
  labels:
    {{- include "security-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "security-mcp-server.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP

